# ---------------------------------------------------------------------------
# DEV / CI lock — runtime closure + the test toolchain.
#
# Use this locally and in CI:  pip install -r requirements-dev.lock.txt
# The production image installs requirements.lock.txt only.
# ---------------------------------------------------------------------------
-r requirements.lock.txt

# ---- Test toolchain ----
fakeredis==2.37.0
iniconfig==2.3.0
pluggy==1.6.0
Pygments==2.20.0
pytest==9.1.1
pytest-django==4.13.0
requests-mock==1.12.1
sortedcontainers==2.4.0
colorama==0.4.6; sys_platform == "win32"
