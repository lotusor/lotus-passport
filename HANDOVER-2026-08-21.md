# 莲花通行证（Lotus Passport）项目接手文档（当前状态 · 2026-08-21）

> 本文是「当前真实状态」接手文档，聚焦 7 个维度。更细的架构 / 端点全表 / 配置全表见同目录 `HANDOVER.md`（115KB 详版，仍保留作详细参考）。
> ✅ 外部应用授权确认页（consent）相关改动**已于 2026-08-25 提交**（commit `0d83122`），工作树已恢复干净，git 与服务器运行版本一致。

---

## 1. 项目背景、技术栈与整体架构

**背景**：eacm.cn 旗下「统一身份认证认证中心」。聚合 GitHub / QQ / 微信的 OAuth，签发统一的 RS256 JWT。业务系统（如 E-algo Rank）只消费令牌，用自己的库关联用户、自决权限。**护照不做业务权限**（不存学校/角色/积分等）。

**技术栈**
- 后端：Django 5.2 + DRF + djangorestframework-simplejwt 5.5.1
- OAuth：authlib（GitHub / 微信标准流程）；QQ 因接口非标（`urlencoded` + `openid` 在 `me` 接口）**手写 `QQProvider`**
- 密码学：cryptography —— AES-256-CBC（第三方 token 落库加密）+ RSA 密钥对（RS256 签名）
- 缓存/状态：Redis（限流 + OAuth state + 令牌吊销，带短 TTL）；测试/DEBUG 自动降级 fakeredis
- 前端 SPA：Next.js 14（`lotus-passport-security`，部署于 `account.eacm.cn`），经同源代理 `app/api/v1/[...path]/route.ts` 避免跨域预检
- 关键决策：**RS256 而非 HS256**——接入方只用公钥验签，私钥只留一台机器；**接入方一律用官方 SDK 离线验签，禁止手写 JWT 校验**（防 alg 混淆攻击）

**整体架构（monorepo）**
```
lotus-passport/                  Django 后端（passport.eacm.cn :8000）
├── passport/                    views / urls / settings / models / providers / jwt / crypto / keys
│                                / ratelimit / redirects / revocation / security / webauthn ...
├── keys/                        RS256 密钥对 + manifest.json（持久卷，丢=全站 JWT 失效）
├── nginx/                       容器 nginx 配置 + 通配证书
├── docker-compose.yml           web(gunicorn) + frontend(Next) + nginx（host 网络）
└── docs/integration/            接入方契约（project1-ealgo-rank.md 等）
lotus-passport-security/         Next.js SPA（account.eacm.cn :3000）登录页/账号中心/同源代理
lotus-passport-sdk/              Python 接入 SDK（离线验签）
lotus-passport-sdk-js/           JS 接入 SDK
```

**数据流**：第三方 OAuth → 护照换码签发 RS256 JWT → 首屏登录直接 302 回跳 fragment；**外部应用接入**则先存一次性票据 302 到 `account.eacm.cn/oauth/consent` 确认页，用户点「授权」后回跳应用（详见 §3）。

---

## 2. 当前代码结构与核心模块职责

**后端 `passport/`**
- `views.py`：所有 API 视图。`OAuthLoginView`（返回 `authorize_url`）、`OAuthCallbackView`（换码→签发→回跳）、`OAuthConsentView`（确认页后端）、`OAuthBindView/UnbindView/AccountsView`、`UserInfoView`、`TokenRefreshView`、`LogoutView`（吊销 jti）、`ProfileView`、`AvatarUploadView`、`Device*View`、`Session*View`、`LoginHistoryView`、`PasswordLoginView/Status/Change`、`Passkey*/WebAuthn*View`、`jwks_view`。
- `urls.py`：`api/v1/oauth/<provider>/login|callback|bind|unbind`、`api/v1/oauth/consent/`（确认页后端）、`/api/v1/oauth/accounts/`、`/api/v1/userinfo/`、`/token/refresh/`、`/login/`、`/security/*`、`/.well-known/jwks.json` 等。
- `settings.py`：密钥/算法（JWT_USE_RS256）、CORS、OAuth 白名单（`OAUTH_ALLOWED_REDIRECT_URIS` / `OAUTH_FIRST_PARTY_ORIGINS` / `OAUTH_CONSENT_PAGE_BASE` / `OAUTH_CLIENTS`）、限流、Redis/PG 连接。
- `models.py`：`PassportUser` + `OAuthAccount`（第三方 token AES 落库）。
- `providers.py`：`GitHubProvider` / `WeChatProvider`（authlib）/ `QQProvider`（手写）。
- `jwt.py` / `keys.py`：RS256 签发 + JWKS 多 `kid` 支持（密钥轮换零掉线）。
- `crypto.py`：AES-256-CBC 第三方 token 加密。
- `ratelimit.py`：`RateLimiter` / `AccountLockout` / `OAuthStateStore` / **`PendingConsentStore`**（确认页一次性票据，TTL 600s）。
- `redirects.py`：`is_redirect_uri_allowed`（防开放重定向）、`is_external_oauth_redirect`、`resolve_oauth_client`（确认页应用信息）。
- `revocation.py` / `security.py` / `webauthn.py` / `auth_events.py` / `captcha.py` / `geo.py` / `middleware.py` / `checks.py`。

**前端 `lotus-passport-security/`**
- `app/login/page.tsx`：账号中心登录页（GitHub/微信/QQ/密码）。
- `app/oauth/consent/page.tsx`：**授权确认页**（新，未提交）—— GET 取应用信息，原生 `<form>` POST 授权/取消，后端 302 让浏览器跨域跳回应用。
- `lib/passport-api.ts`：API 封装，含 `getOAuthConsentInfo(ticket)`。
- `lib/auth-context.tsx`、`app/api/v1/[...path]/route.ts`（同源代理）。

---

## 3. 已完成的任务及进度百分比

| 模块 | 状态 | 进度 | 说明 |
|---|---|---|---|
| OAuth 登录（GitHub / QQ；微信留配置位） | ✅ | 100% | authlib + 手写 QQProvider；GitHub/QQ 凭证均已配好可真实登录 |
| 密码登录 + 安全（失败锁定/限流/异常检测） | ✅ | 100% | §9.4a |
| Passkey / WebAuthn | ✅ | 100% | §9.4b |
| 设备/会话管理、登录历史 | ✅ | 100% | §9.3（含 trust-gate 安全修复） |
| 资料/头像、账号注销（级联+审计） | ✅ | 100% | §9.1 / §9.4f |
| RS256 签名 + JWKS + 密钥轮换（零掉线） | ✅ | 100% | |
| 接入方 SDK（Python / JS，离线验签） | ✅ | 100% | |
| 外部应用授权确认页（consent） | ✅ | 100% | 后端 `OAuthConsentView`/`PendingConsentStore`/`redirects` + 前端确认页，**已 commit（2026-08-25）** |
| QQ OAuth 回调 | ✅ | 100% | 代码已实现（手写 QQProvider，含无尾斜杠回调兼容），凭证已配好、可真实登录 |
| 2FA（TOTP） | ❌ 去范围 | 0% | 迁移 0005 已移除，当前 404 |
| 邮件/短信通知网关 | ❌ 未做 | 0% | 密码重置等依赖项暂缓 |
| 微信 OAuth 真·交换 | ⏳ 阻塞 | 0% | 需腾讯开放平台登记可信域名（见 §5） |

**整体项目进度约 90%**（身份中心功能基本闭环，GitHub/QQ 均可真实登录，通信/通知类与微信真接入待外部前置）。

---

## 4. 进行中与待处理的任务清单及优先级

**P0（先处理）**
- ✅ **提交 consent 相关未提交改动**（2026-08-25 已完成，commit `0d83122`）。
- **护照→GitHub 出站 504 排查**：真·第三方交换偶发超时（设施/网络层），阻塞真人登录联调。

**P1**
- 微信 OAuth 真·接入：腾讯开放平台要求网站正式上线并登记可信域名后方可申请/配置回调，当前仅留配置位。（QQ 已完成，凭证已配好）
- 安全回归验证：近期连续 trust-gate 修复（§9.3）建议补充端到端测试，确认无回归。

**P2**
- 邮件 / 短信通知网关（§9.7）：解锁密码重置（§9.4a `reset`）、异地登录告警等。
- 密码重置（邮件重置）流程补全。
- 监控接入：当前无内置 APM，建议 nginx/compose 层接入外部监控（health、`dev_status` 的 `dev_login_enabled` 应为 false）。

---

## 5. 已知问题、技术债务与风险点

- ✅ **未提交改动风险（已解决）**：consent 页整套改动已 commit（2026-08-25），git 与服务器一致。
- **护照→GitHub 出站 504**：真·第三方交换偶发超时，属设施/网络问题，需排查（影响所有依赖 GitHub 登录的接入方联调）。
- **微信 OAuth 未配置**：受腾讯开放平台管理要求（需正式上线 + 可信域名登记），当前 `WECHAT_CLIENT_ID/SECRET` 多为空；QQ 已完成（凭证已配好）。
- **JWT 不签发 `aud`**：防御盲区（接入方离线验签不校验 audience）。
- **Next.js 14.2.35 已知 2 个 high 公告**：已评估为可接受风险（§7.7），持续关注升级。
- **RS256 密钥丢失 = 全站 JWT 失效**：`keys/` 卷必须离线备份；JWKS 多 `kid` 仅缓解轮换不缓解丢失。
- **服务器 root 密码曾在聊天暴露**：部署后务必 `passwd root` 更换。
- **2FA 已去范围**：迁移 0005 移除，相关端点 404（历史设计残留，勿误用）。
- **密码重置未做**：仅 set/change，依赖通知网关的 reset 暂缓。

---

## 6. 环境配置、部署流程与关键账号信息

**域名体系**：`passport.eacm.cn`（API :8000）、`account.eacm.cn`（SPA :3000），同 nginx 按 server_name 分流，TLS 用 `*.eacm.cn` 通配证。

**核心环境变量**（`lotus-passport/.env.production`）
- `DATABASE_URL` / `REDIS_URL`：连裸机 PG / Redis（强密码）。
- `PASSPORT_OAUTH_REDIRECT_BASE=https://passport.eacm.cn/api/v1/oauth`
- `FRONTEND_SUCCESS_REDIRECT=https://account.eacm.cn/`
- `GITHUB_CLIENT_ID` / `_SECRET`、`WECHAT_CLIENT_ID` / `_SECRET`、`QQ_CLIENT_ID` / `_SECRET`
- `OAUTH_FIRST_PARTY_ORIGINS=https://account.eacm.cn,https://passport.eacm.cn`
- `OAUTH_CONSENT_PAGE_BASE=https://account.eacm.cn/oauth/consent`
- `OAUTH_CLIENTS`（按 origin 索引，已含 `https://rank.eacm.cn` → E-algo Rank + 权限范围）
- `OAUTH_ALLOWED_REDIRECT_URIS`：含 `https://rank.eacm.cn`（origin 级）
- 前端 `PASSPORT_API_ORIGIN`：服务端专用（故意不带 `NEXT_PUBLIC_`），compose 内填 `http://127.0.0.1:8000`。

**部署拓扑**（`docker-compose.yml`，host 网络）
- `web`：Django + gunicorn，entrypoint 自动「等 PG → 首次生成 RSA 密钥 → collectstatic → migrate → gunicorn」；healthcheck 用 `/api/v1/dev/status/`。
- `frontend`：Next.js 构建产物，监听 `:3000`。
- `nginx`：监听宿主 80/443，复用 `*.eacm.cn` 通配证，反代 web/frontend。
- 卷：`passport_keys`（RS256 密钥，**必须离线备份**）、`passport_media`（头像等）。

**部署命令（服务器上）**
```bash
cd <repo>/lotus-passport/lotus-passport
docker compose up -d --build web frontend
docker compose logs -f web
```

**未提交改动（已于 2026-08-25 全部提交）**
- commit `0d83122`：consent 确认页（后端 `views/urls/settings/ratelimit/redirects` + 前端 `app/oauth/consent/page.tsx` + `lib/passport-api.ts`）
- commit `021ef33`：前端登录页占位入口（`login/page.tsx`、`login/password/page.tsx`、`components/modal.tsx`）+ README/契约文档

**关键账号 / 凭证**
- 裸机 PostgreSQL（`lotus_passport` 库/角色）+ Redis（强密码）。
- 服务器 root：聊天记录曾暴露，**部署后 `passwd root` 更换**。
- GitHub OAuth App（Client ID/Secret）已配置；QQ OAuth 已完成（凭证已配好）；微信待腾讯开放平台申请。
- RS256 私钥仅在 `passport_keys` 卷，务必离线备份。

---

## 7. 相关文档与联系人

- **本仓库 `HANDOVER.md`**（115KB 详版）：架构 / 端点全表 / 配置全表 / 维护指南 / 扩展改造注意事项，接手详细参考首选。
- **`docs/integration/project1-ealgo-rank.md`**：E-algo Rank 接入契约（护照方维护，rank 只消费）。
- **`README.md` / `PRE_LAUNCH_VERIFICATION.md`**：上手与上线核查。
- **联系人**：项目 owner / 维护方 = JXGM（同一团队维护护照与 rank 两侧）。
- **关键外部依赖**：GitHub OAuth、腾讯开放平台（QQ 已完成 / 微信待申请）、eacm.cn 域名/DNS/通配证书、裸机 PG/Redis、宝塔面板（若复用）。
